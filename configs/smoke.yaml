episodes: 2
gamma: 0.99
learning_rate: 0.001
batch_size: 2
replay_buffer_size: 50
min_replay_size: 2
target_update_every: 2
epsilon_start: 1.0
epsilon_end: 0.1
epsilon_decay: 0.5
checkpoint_every: 1
eval_every: 1
eval_episodes: 1
max_steps_per_episode: 3
seed: 42
